// 1) define a variable to get values from body paerse

// 2) define two variable to store function for all Reg and filtering function

// 3) loop through the variable that contain all the towns/registrations;

        // compare current index to varibable coming from body parser
        // if found assign a new key, call it what ever you want and the assign it true 
    // return that current index



    // City ID is undefined.

    // The filter function does work. The get route as well.

    